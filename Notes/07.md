## 7.1

- compound statements/blocks
- i'm already comfortable with this
- example
```cpp
#include <iostream>

int main()
{ // block 1, nesting level 1
    std::cout << "Enter an integer: ";
    int value {};
    std::cin >> value;

    if (value >  0)
    { // block 2, nesting level 2
        if ((value % 2) == 0)
        { // block 3, nesting level 3
            std::cout << value << " is positive and even\n";
        }
        else
        { // block 4, also nesting level 3
            std::cout << value << " is positive and odd\n";
        }
    }

    return 0;
}
```

## 7.4

- use global variables

- im lowkey already comfortable with scope and like what variables are local and global so im scrolling through

## 7.5

- make sure to not declare a local variable if u have a global variable with the same fucking name dipshit idiot

## 7.13 

> using namespace std;
- lets u use cout or cin without typing std::

