## 3.1

- syntax error is when u syntax err so like dont miss stuff
- semantic error is when something is valid but breaks a rule such as trying to define "hello" as an int or trying to define a variables value to a variable that hasnt been declared 
- more often you just get unexpected behaviour like when you try to print a variable that was declared but not initialized or like when return runs before your code or typing the wrong code lol

## 3.2

- how to debug?
1. find the cause
2. understand why 
3. determine a fix
4. repair the issue
5. retest

- seems pretty trivial, an example given is 
```cpp
#include <iostream>

int add(int x, int y) // this function is supposed to perform addition
{
    return x - y; // but it doesn't due to the wrong operator being used
}

int main()
{
    std::cout << "5 + 3 = " << add(5, 3) << '\n'; // should produce 8, but produces 2

    return 0;
}
```
- so you see the operator is wrong and then fix it 

## 3.3 

- strategy for debugging
> Looking at every line of code in a program that is thousands of lines long can take a really long time (not to mention it’s incredibly boring). Second, the code itself tends to be more complex, with more possible places for things to go wrong. Third, the code’s behavior may not give you many clues as to where things are going wrong. If you wrote a program to output stock recommendations and it actually output nothing at all, you probably wouldn’t have much of a lead on where to start looking for the problem.

- run the program to find problems, figure out which part its breaking at. i mean me personally id just add a std::cout to print if something didnt work i guess
>  However, sometimes reproducing an issue can be a lot more difficult. The problem may only occur on certain computers, or in particular circumstances (e.g. when the user enters certain input). In such cases, generating a set of reproduction steps can be helpful.

## 3.4

- strategy 1: comment out stuff

- strategy 2: std::cout something to see what is running and what isnt? ok lets see their example
```cpp
#include <iostream>

int getValue()
{
	return 4;
}

int main()
{
    std::cout << getValue << '\n';

    return 0;
}
```

- now apparently this sometimes prints 00101424 instead of 1 (and i think that is fixed if you just type `getValue()` instead of just getValue) 

```cpp
#include <iostream>

int getValue()
{
std::cerr << "getValue() called\n";
	return 4;
}

int main()
{
std::cerr << "main() called\n";
    std::cout << getValue << '\n';

    return 0;
}
```

- now the output is main() called \n 1 which shows that the get value function isnt being rn since you didnt see the getvalue() called message

- strategy 3: printing values
- just print the variables that are being calculated wrong to identify where therye being fucked up
```cpp
#include <iostream>

int add(int x, int y)
{
std::cerr << "add() called (x=" << x << ", y=" << y << ")\n";
	return x + y;
}

void printResult(int z)
{
std::cerr << "printResult() called (z=" << z << ")\n";
	std::cout << "The answer is: " << z << '\n';
}

int getUserInput()
{
std::cerr << "getUserInput() called\n";
	std::cout << "Enter a number: ";
	int x{};
	std::cin >> x;
	return --x;
}

int main()
{
std::cerr << "main() called\n";
	int x{ getUserInput() };
std::cerr << "main::x = " << x << '\n';
	int y{ getUserInput() };
std::cerr << "main::y = " << y << '\n';

	int z{ add(x, y) };
std::cerr << "main::z = " << z << '\n';
	printResult(z);

	return 0;
}
```
- here you see the issue whenever the getuserinput is called you're getting 1 less than what you input, because you did return --x
> The --x is the pre-decrement operator, which decrements x by 1 before returning it

## 3.5 

- MORE debugging tactics? bro fuck off

```#include <iostream>

#define ENABLE_DEBUG // comment out to disable debugging

int getUserInput()
{
#ifdef ENABLE_DEBUG
std::cerr << "getUserInput() called\n";
#endif
	std::cout << "Enter a number: ";
	int x{};
	std::cin >> x;
	return x;
}

int main()
{
#ifdef ENABLE_DEBUG
std::cerr << "main() called\n";
#endif
    int x{ getUserInput() };
    std::cout << "You entered: " << x << '\n';

    return 0;
}
```

- that sounds inefficient

- a LOGGER. now this sounds useful, good

```cpp
#include <plog/Log.h> // Step 1: include the logger headers
#include <plog/Initializers/RollingFileInitializer.h>
#include <iostream>

int getUserInput()
{
	PLOGD << "getUserInput() called"; // PLOGD is defined by the plog library

	std::cout << "Enter a number: ";
	int x{};
	std::cin >> x;
	return x;
}

int main()
{
	plog::init(plog::debug, "Logfile.txt"); // Step 2: initialize the logger

	PLOGD << "main() called"; // Step 3: Output to the log as if you were writing to the console

	int x{ getUserInput() };
	std::cout << "You entered: " << x << '\n';

	return 0;
}
```
- from what i can see, its initializing a logger, then outputting everything to a log file, every neat i like this. to turn off logging you can replace plog::debug with plog::none apparently

## 3.6

- they made me setup a debugger on vsc?
> Then open main.cpp and start debugging by pressing F5 or by pressing Ctrl+Shift+P and selecting “Debug: Start Debugging and Stop on Entry”.

- lemme just test this out with some code then 

## 3.7

- this is a snoozefest bro 
- i get it, debugging in vscode seems quite straightforward, leave me alone

## 3.8

- i dont like this ill just ask ai how to debug when i get to a need of debugging, 
- ill never need debugging if **i write correct code**
- and even if i make an error while coding, i will just look at the code myself? like ok lets see if i can identify the issue quicker than the time itd take to write debugging for the quiz questions in the summary chapter

## 3.x

- quiz questions

```cpp
#include <iostream>

int readNumber(int x)
{
	std::cout << "Please enter a number: ";
	std::cin >> x;
	return x;
}

void writeAnswer(int x)
{
	std::cout << "The sum is: " << x << '\n';
}

int main()
{
	int x {};
	readNumber(x);
	x = x + readNumber(x);
	writeAnswer(x);

	return 0;
}
```
- this is stupid are you trying to double your answer? it says sum so maybe its trying to add? either way the issue is that there is no fucking readNumber defined, itd ask you an input twice, once on line 233 then discard the first input (it is saved yeah but theres nothing done with it) then the second time the function is ran on 234, x here is zero so that line is just saying x = 0 + the second number you input, and say your answer is the second input you put in,  and that took 40 seconds to identify
- the correct fix would be obviously setting a num variable to the first readNUmber(x) function then you can do sum = num + readNumber(x); and writeAnswer(sum);
- or you can just not change anything and initiate the variable x with int x { readNumber() }; so itd call the function and itd set the x so the next line would be x = the set value + new value, which would be the sum
> The following program is supposed to divide two numbers,
```cpp
#include <iostream>

int readNumber()
{
	std::cout << "Please enter a number: ";
	int x {};
	std::cin >> x;
	return x;
}

void writeAnswer(int x)
{
	std::cout << "The quotient is: " << x << '\n';
}

int main()
{
	int x{ };
	int y{ };
	x = readNumber();
	x = readNumber();
	writeAnswer(x/y);

	return 0;
}
```

- x is set twice, 19 seconds to identify, if you replace one of the x with a y it should work, or just initialize the functions with 	int x{ readNumber() }; and int y{ readNumber() }; cuz thatd do both running the function once and setting the functions value 

> What does the call stack look like in the following program when the point of execution is on line 4? Only the function names are needed for this exercise, not the line numbers indicating the point of return.
```cpp
#include <iostream>

void d()
{ // here
}

void c()
{
}

void b()
{
	c();
	d();
}

void a()
{
	b();
}

int main()
{
	a();

	return 0;
}
```
- first calls A, A calls B, B calls C then D, just trace your code back? 10 seconds.
- okay ill admit i got this confused because i didnt know what a "call stack" was cuz i skipped past the definition
> The call stack is a list of all the active functions that have been called to get to the current point of execution. The call stack includes an entry for each function called, as well as which line of code will be returned to when the function returns. Whenever a new function is called, that function is added to the top of the call stack. When the current function returns to the caller, it is removed from the top of the call stack, and control returns to the function just below it.
- so in the order of last executed specifically to call the a line within D, first itd be D then B then A then main cuz thats whats leading up to a line inside D, 2 mins 20 seconds cuz i had to also understand what a call stack is

> Extra credit: The following program is supposed to add two numbers, but doesn’t work correctly.
- "use a debugger blah blah blah" fuck you no

```cpp
#include <iostream>

int readNumber()
{
    std::cout << "Please enter a number: ";
    char x{};
    std::cin >> x;

    return x;
}

void writeAnswer(int x)
{
    std::cout << "The sum is: " << x << '\n';
}

int main()
{
    int x { readNumber() };
    int y { readNumber() };
    writeAnswer(x + y);

    return 0;
}
```

- x isnt being initialized as a number in readNumber() its being initialized as a char (which is probably a string or something? something we likely werent taught yet). i dont know what a char is but a fix is just setting it to int ? thats so obvious. 15 seconds.


- FUCK DEBUGGING
