## 2.1

- what is a function? a function is a reusable sequence of statements designed to do a particular job
- pretty much like the main function except main executes whenever the script is ran and the function executes whenever called in main or something like that
> The C++ standard library comes with plenty of already-written functions for you to use -- however, it’s just as common to write your own. Functions that you write yourself are called user-defined functions.
- the syntax is like 
```cpp
returnType functionName() // This is the function header (tells the compiler about the existence of the function)
{
    // This is the function body (tells the compiler what the function does)
}
```
- the returnType is usually int or void or something we'll get to return types next sub chapter apparently
- just like the main function, stuff in the curly braces are what is being ran in sequence, to call a function you just add `functionName()` in your code apparently
```cpp
#include <iostream> // for std::cout

// Definition of user-defined function doPrint()
// doPrint() is the called function in this example
void doPrint()
{
    std::cout << "In doPrint()\n";
}

// Definition of user-defined function main()
int main()
{
    std::cout << "Starting main()\n";
    doPrint();                        // Interrupt main() by making a function call to doPrint().  main() is the caller.
    std::cout << "Ending main()\n";   // This statement is executed after doPrint() ends

    return 0;
}
``` 
- very simple, let me try to make something myself lowkey
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number to multiply by 2: ";
    std::cin >> number;
    std::cout << "The result is: " << number * 2 << "\n";
    
}

int main()
{
    std::cout << "running main function \n";
    multiplyby2();
    std::cout << "main function is done \n";
    return 0;
}
```
- that did not work, it requires a return value in the multiplyby2 function? oh wait they did void doPrint() earlier so void probably means it doesnt need a return value lemme change it to void and try
- yeah that worked
- you can call the function more than once in the main function, obviously
- and you can have functions that call other functions that call other functions, it makes sense and it seems like the order in which the functions are defined matters? i mean you cant call something that doesnt exist right? idk
- apparently you cannot nest functions, like you cannot define a function inside another function, that makes sense
```cpp
#include <iostream>

int main()
{
    void foo() // Illegal: this function is nested inside function main()
    {
        std::cout << "foo!\n";
    }

    foo(); // function call to foo()

    return 0;
}
```
- youd have to define foo() outside 

## 2.2 

- the returnTypes of the functions (like int or void we saw earlier)
- theyre rewriting the old double this number script using a function to double the number using an int return type function, i havent looked at their code yet but i am going to try this 
- huhh i just discovered if you run a function that has a std::cout and then std::cout << thatFunction then itll print the std::cout then print the functions int value 
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number: ";
    std::cin >> number;
    return number * 2;
    
}

int main()
{
    std::cout << "running main function \n";
    std::cout <<  multiplyby2() << "\n"; 
    std::cout << "main function is done \n";
    return 0;
}
```
- but what if i wanted the output to be like `Enter a number: 4 \n The function's int value is: 8 \n main function is done`
- i would have to put that in the multiplyby2 function i assume ? 
- theres something im missing (which would probably be taught continuing this chapter) that should let me do that because if i do this: 
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number: ";
    std::cin >> number;
    return number * 2;
    
}

int main()
{
    std::cout << "running main function \n";
    multiplyby2();
    std::cout << "main function is done \n";
    return 0;
}
```
- i need a way to run the function and then print just the value of the function, 
- wait since an int function is really just a variable with an integer value and since you can do this: 
```cpp
    int x { 5 };
    int y { x };
```
- you can initiate a variable with the value of another variable then you should be able to initiate a variable with a function
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number: ";
    std::cin >> number;
    return number * 2;
    
}

int main()
{
    std::cout << "running main function \n";
    multiplyby2();
    int num { multiplyby2() };
    std::cout << "The function's int value is: " << num << "\n" << "main function is done \n";
    return 0;
}
```
- that is still giving me the "enter a number" twice, but i get the int value seperately, so i conclude even if i dont directly call the function on a line as a statement like `function();` as a line and simply just mention the function anywhere in the code or declare a variable with the functions value, the function is still ran 
- so if i just do this:
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number: ";
    std::cin >> number;
    return number * 2;
    
}

int main()
{
    std::cout << "running main function \n";
    int num { multiplyby2() };
    std::cout << "The function's int value is: " << num << "\n" << "main function is done \n";
    return 0;
}
```
- i get the result i wanted
- okay going back to the course, they did pretty mcuh exactly what i came up with or concluded
- you cannot call a main function in another function apparently, ok, C lets you do this though which is cool i guess?
- main function has to be int
> Your main function should return the value 0 if the program ran normally.
- best practice is to make it return 0 and this way youd be able to debug or something yeah, you can also use return EXIT_SUCCESS; apparently, i think ill just use 0 
> Make sure your functions with non-void return types return a value in all cases.
> Failure to return a value from a value-returning function will cause undefined behavior.
- yeah makes sense, thatd happen if i remove the `return number * 2` line froom my function
- the main function you dont have to write return 0; for it to return 0 it will return zero but its best practice to write it
> A value-returning function can only return a single value back to the caller each time it is called.
- but we can dictate what that single value can be like how we're deciding that single value in my `multiplyby2()` by taking a user input
- reusing functions! theyre going to want to reuse a function to get a user input instead of typing one thing over and over again for example 
```cpp
#include <iostream>

int main()
{
	int x{};
	std::cout << "Enter an integer: ";
	std::cin >> x;

	int y{};
	std::cout << "Enter an integer: ";
	std::cin >> y;

	std::cout << x << " + " << y << " = " << x + y << '\n';

	return 0;
}
```
- you dont want to type "enter an integer" over and over again cuz thats bad a good rule is "dont repeat yourself" so to replace it with a function (lemme just try to write this instead of looking at the course) 
```cpp
#include <iostream>

int getInteger()
{
    int value{};
    std::cout << "Enter an integer: ";
    std::cin >> value;
    return value;
}

int main()
{
	int x{ getInteger() };

	int y{ getInteger() };

	std::cout << x << " + " << y << " = " << x + y << '\n';

	return 0;
}
```
- thats what i came up with, the functions get called even when initializing a variable with that function's value
- the course came up with the same thing 
>Follow DRY: “Don’t repeat yourself”. If you need to do something more than once, consider how to modify your code to remove as much redundancy as possible. Variables can be used to store the results of calculations that need to be used more than once (so we don’t have to repeat the calculation). Functions can be used to define a sequence of statements we want to execute more than once. And loops (which we’ll cover in a later chapter) can be used to execute a statement more than once.
>Like all best practices, DRY is meant to be a guideline, not an absolute. Reader Yariv has noted that DRY can harm overall comprehension when code is broken into pieces that are too small.
- also the course says when theres something like this:
```cpp
int getNumbers()
{
    return 5;
    return 7;
}
```
- the function takes the first return value it can find and just stops cuz it was told to return, so the value will stay 5 no matter how many times u execute this function
## 2.3
- covering void functions, we talked about int functions earlier
- void functions dont need a return statement and they cant be used to initialize an int variable like int x{ getInteger() }; like we did earlier
- they can however be ran, example from course:
```cpp
#include <iostream>

// void means the function does not return a value to the caller
void printHi()
{
    std::cout << "Hi" << '\n';

    // This function does not return a value so no return statement is needed
}

int main()
{
    printHi(); // okay: function printHi() is called, no value is returned

    return 0;
}
```
- pretty straight forward, prints hi 
> Do not put a return statement at the end of a non-value returning function. (even though you can put a return; it doesnt matter and trying to return a value is a compile error)
>> Void functions can’t be used in expression that require a value
- yeah i concluded, you can put printHi(); on a line by itself but `std::cout << PrintHi();` would do nothing cuz it cant print nothing cuz theres no return value for the printHi function
- short sub chapter, gonna take a break here 

## 2.4

- they introduced a problem where what if you wanted to turn the console output for the code we wrote on line 156 also into a function. 
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number: ";
    std::cin >> number;
    return number * 2;
    
}

int main()
{
    int num { multiplyby2() };
    std::cout << "The function's int value is: " << num << "\n" << "main function is done \n";
    return 0;
}
```
- so we have that, what could we do to also turn the output into a function, i have no idea i cant think of anything
- function parameters. 
- a function parameter is something you put in the brackets when typing out a function like we typed int multiplyby2() if you typed int multiplyby2( int z ) the int z would be a function parameter, i dont see what you could do with this tho
- oh shitt you can apparently pass a value to the function with an arguement uhh i dont know how to explain let me just edit my code to show
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number: ";
    std::cin >> number;
    return number * 2;
    
}

void printMessage(int num)
{
    std::cout << "The function's int value is: " << num << "\n";
}

int main()
{
    int num { multiplyby2() };
    printMessage(num);
    return 0;
}
```
- yeah see, you can define a variable and pass that variable to a function, thats pretty neat, you can even do two function parameters
```cpp
void printValues(int x, int y)
{
    std::cout << x << '\n';
    std::cout << y << '\n';
}

int main()
{
    printValues(6, 7); // This function call has two arguments, 6 and 7

    return 0;
}
``` 
- now i have this, i should try turning my original two number adder script into a function with function parameters, hold up
```cpp
#include <iostream>

void printMessage(int sum)
{
    std::cout << "The sum is: " << sum << "\n";
}

void addTwoNumbers(int x, int y)
{
    //  int x{}, y{}; // do you not need to declare x and y here because they are already declared in the function parameters? yeah you dont
    std::cout << "Enter two numbers with a space: ";
    std::cin >> x >> y;
    printMessage(x + y);

}

int main()
{
    addTwoNumbers(0, 0);  // it wont matter what i put here because the function will ask for user input
}
```
- i learnt you dont need to declare a variable thats declared in the function parameters and achieved my goal of making a number adder function and a function that does the console output for the number adder, i could've actually just put it all in 1 function but eh

- you can use a return value of a function as an arguement, i havent looked at their code but you could simplify it more by removing the num variable completely since it was only used once like this:
```cpp
#include <iostream>

int multiplyby2()
{
    int number{};
    std::cout << "Enter a number: ";
    std::cin >> number;
    return number * 2;
    
}

void printMessage(int num)
{
    std::cout << "The function's int value is: " << num << "\n";
}

int main()
{
    printMessage(multiplyby2());
    return 0;
}
```
- yeah that works, also you can pretty much make your own operators like + or * with a function
```cpp
int add(int x, int y)
{
    return x + y;
}

int multiply(int z, int w)
{
    return z * w;
}

int main()
{
    std::cout << add(4, 5) << '\n'; // within add() x=4, y=5, so x+y=9
    std::cout << add(1 + 2, 3 * 4) << '\n'; // within add() x=3, y=12, so x+y=15

    int a{ 5 };
    std::cout << add(a, a) << '\n'; // evaluates (5 + 5)

    std::cout << add(1, multiply(2, 3)) << '\n'; // evaluates 1 + (2 * 3)
    std::cout << add(1, add(2, 3)) << '\n'; // evaluates 1 + (2 + 3)

    return 0;
}
```
- pretty straight forward 
- unnamed parameters. you can type `void doSomething (int)` or `void doSomething (int /*parameter*/)` and they wont cause a compilation error, but i dont really get what theyd be used for neither have they explained well (and i am not reading blocks of text)

- assignments: Write a function called doubleNumber() that takes one integer parameter. The function should return double the value of the parameter.
```cpp
#include <iostream>

int doubleNumber(int x) {
    return x*2;
}

int main() {
    std::cout << doubleNumber(4);
}
```
- next one: Write a complete program that reads an integer from the user, doubles it using the doubleNumber() function you wrote in the previous quiz question, and then prints the doubled value out to the console.
```cpp
#include <iostream>

int doubleNumber(int x) {
    return x*2;
}

int main() {
    std::cout << "Enter a number: ";
    int num{};
    std::cin >> num;
    std::cout << "Your doubled number is: " << doubleNumber(x); 
}
```
- i think thats the most straightforward way to solve it i dont see a way to NOT define the num variable here tbh (atleast because they mentioned i should use the exact doubleNumber() function i wrote earlier) and yep they did it the same way

## 2.5 

- variables defined inside a function are local variables and it seems they only stay defined within that function, makes perfect sense 
```cpp
int add(int x, int y) // function parameters are also generally a local variable // x and y are created and initialized here
{
    int z{ x + y }; // z is a local variable // z is created and initialized here
    
    return z;
} // x y and z are deleted here, so the life time is just how long the function takes, and obviously x y and z are only usable within this function
```
- there is also a "scope" basically determining if the variable is usable, and it is in scope if it is initialized and alive 
- we say z goes out of scope by the end of the function add and imagine a q, q isnt in scope anywhere in that function because it has never been initialized
```cpp
#include <iostream>

int add(int x, int y) // x and y are created and enter scope here
{
    // x and y are usable only within add()
    return x + y;
} // y and x go out of scope and are destroyed here

int main()
{
    int a{ 5 }; // a is created, initialized, and enters scope here
    int b{ 6 }; // b is created, initialized, and enters scope here

    // a and b are usable only within main()

    std::cout << add(a, b) << '\n'; // calls add(5, 6), where x=5 and y=6

    return 0;
} // b and a go out of scope and are destroyed here
```
- this all pretty much makes sense to me but they keep yapping about it in the course subchapter
- and it is obvious to me that the function main can have an x and y and the function add can have an x and y but each's x and y's are only valid within their function so code like this:
```cpp
#include <iostream>

int add(int x, int y) // add's x and y are created and enter scope here
{
    // add's x and y are visible/usable within this function only
    return x + y;
} // add's y and x go out of scope and are destroyed here

int main()
{
    int x{ 5 }; // main's x is created, initialized, and enters scope here
    int y{ 6 }; // main's y is created, initialized, and enters scope here

    // main's x and y are usable within this function only
    std::cout << add(x, y) << '\n'; // calls function add() with x=5 and y=6

    return 0;
} // main's y and x go out of scope and are destroyed here
```
- is completely valid because theyre not in conflicting scope
> Define your local variables as close to their first use as reasonable.
- when deciding whether to use a local variable as a function parameter or a local variable initialization
> When a variable is needed within a function:
>
>>Use a function parameter when the caller will pass in the initialization value for the variable as an argument.
>>Use a local variable otherwise.
- the less variables defined and used the better basically: 
```cpp
 #include <iostream>

int getValueFromUser(int val) // val is a function parameter
{
    std::cout << "Enter a value: ";
    std::cin >> val;
    return val;
}

int main()
{
    int x {}; // this is bad because we're adding x and just using x for 1 thing when we couldve just not done that 
    int num { getValueFromUser(x) }; // main must pass x as an argument

    std::cout << "You entered " << num << '\n';

    return 0;
}
```
- this is optimal 
```cpp
#include <iostream>

int getValueFromUser()
{
    int val {}; // val is a local variable
    std::cout << "Enter a value: ";
    std::cin >> val;
    return val;
}

int main()
{
    int num { getValueFromUser() }; // main does not need to pass anything

    std::cout << "You entered " << num << '\n';

    return 0;
}
```
- there are "temporary objects" like in this code: 
```cpp
#include <iostream>

int getValueFromUser()
{
 	std::cout << "Enter an integer: ";
	int input{};
	std::cin >> input;

	return input; // return the value of input back to the caller
}

int main()
{
	std::cout << getValueFromUser() << '\n'; // where does the returned value get stored?

	return 0;
}
```
- the return value is a temporary object 
> Temporary objects have no scope at all (this makes sense, since scope is a property of an identifier, and temporary objects have no identifier).
> Temporary objects are destroyed at the end of the full expression in which they are created. This means temporary objects are always destroyed before the next statement executes.
> In our example above, the temporary object created to hold the return value of getValueFromUser() is destroyed after std::cout << getValueFromUser() << '\n' executes.
- i am going to trust that i need to know all this bullshit to code well

## 2.6

- a function should perform one and only one task, generally
- stuff that appears more than once in a program should be a function so its easier to edit in one place rather than editing all the places stuff appears
- code that has a defined input or output should be a function so its reusable 
- you can have "sub functions" basically call a function within a function if stuff gets too complicated i guess

## 2.7

- remember how i talked about c++ being a sequence and stuff that doesnt exist cant be called earlier? they showed an example of that, when i just deduced that
```cpp
#include <iostream>

int main()
{
    std::cout << "The sum of 3 and 4 is: " << add(3, 4) << '\n';
    return 0;
}

int add(int x, int y)
{
    return x + y;
}
```
- i knew this wouldnt compile because add should be defined first before using add, so add should be up top
> When addressing compilation errors or warnings in your programs, resolve the first issue listed and then compile again.

- they have another solution for this, adding a "forward declaration" basically telling the compiler the existance of an identifier before the sequence gets to it, 
```cpp
#include <iostream>

int add(int x, int y); // forward declaration of add() (using a function declaration)

int main()
{
    std::cout << "The sum of 3 and 4 is: " << add(3, 4) << '\n'; // this works because we forward declared add() above
    return 0;
}

int add(int x, int y) // even though the body of add() isn't defined until here
{
    return x + y;
}
```
- now apparently that works, it is worth noting that on line 605, `int add(int, int)` will also work as a forward declaration but its best practice to 
> Keep the parameter names in your function declarations.
- now i dont see a point of this me personally id just put it above where im using it so its easier or makes more sense or whatever 
> Reordering isn’t possible in this scenario because the caller and the callee are in completely different files! We’ll discuss this in more detail in the next lesson (2.8 -- Programs with multiple code files).
- ohhh okay
> Less often, there are times when we have two functions that call each other. Reordering isn’t possible in this case either, as there is no way to reorder the functions such that each is before the other. Forward declarations give us a way to resolve such circular dependencies.
> Forward declarations can also be used to define our functions in an order-agnostic manner. This allows us to define functions in whatever order maximizes organization (e.g. by clustering related functions together) or reader understanding.
- anyways, if you add a forward declaration but forget to write the actual function, the code wotn compile 
- also a "definition" is a function AND its code, a "declaration" is just telling the compiler about the existance of something like:
```cpp
int x;
int add(int x, int y); // these two are declarations

int x {};
int add(int x, int y) {
    return x+y;            // these two are definitions
}
```
- the "one definition rule"
- within a file, one thing can only have one definition, local variables or functions defined within different namespaces dont break this rule (this is obvious, you define int add once, why define it again?)
> Types, templates, inline functions, and inline variables are allowed to have duplicate definitions in different files, so long as each definition is identical. We haven’t covered what most of these things are yet, so don’t worry about this for now -- we’ll bring it back up when it’s relevant.

## 2.8

- we get to add multiple code files to our project!
> When you add new code files to your project, give them a .cpp extension.
- basically, you can use a forward declaration and put your function in a whole different file, for example:
- file 1: add.cpp
```cpp
int doubleNumber(int x) {
    return x*2;
}
```
- file 2: main.cpp
```cpp
#include <iostream>

int doubleNumber(int x);

int main() {
    std::cout << doubleNumber(4);
}
```
- now this will compile (as long as you added the file in your tasks.json) 
- and this is pretty cool because it works pretty much the same as your main cpp file, awesome

## 2.9

> C++ requires that all identifiers be non-ambiguous. If two identical identifiers are introduced into the same program in a way that the compiler or linker can’t tell them apart, the compiler or linker will produce an error. This error is generally referred to as a naming collision 
- that makes sense, cuz if you define the same function twice, even in two different files, whats the point
- just make sure not to, if its one file then youll remember not to but with multiple files u just gotta take care of that iguess

- on the other hand, if the same function is in two different scope regions, it wont be an issue
> A scope region is an area of source code where all declared identifiers are considered distinct from names declared in other scopes (much like the cities in our analogy). Two identifiers with the same name can be declared in separate scope regions without causing a naming conflict. However, within a given scope region, all identifiers must be unique, otherwise a naming collision will result.

> The body of a function is one example of a scope region. Two identically-named identifiers can be defined in separate functions without issue -- because each function provides a separate scope region, there is no collision. However, if you try to define two identically-named identifiers within the same function, a naming collision will result, and the compiler will complain.
- they didnt give me an example here tho
> A namespace provides another type of scope region (called namespace scope) that allows you to declare or define names inside of it for the purpose of disambiguation. The names declared in a namespace are isolated from names declared in other scopes, allowing such names to exist without conflict. For example, two functions with identical declarations can be defined inside different namespaces, and no naming collision or ambiguity will occur.
- ok bro how do i get a namespace
> Namespaces are often used to group related identifiers in a large project to help ensure they don’t inadvertently collide with other identifiers. For example, if you put all your math functions in a namespace named math, then your math functions won’t collide with identically named functions outside the math namespace.

- everything we've coded so far has been in the general name space
```cpp
#include <iostream> // imports the declaration of std::cout into the global scope

// All of the following statements are part of the global namespace

void foo();    // okay: function forward declaration
int x;         // compiles but strongly discouraged: non-const global variable definition (without initializer)
int y { 5 };   // compiles but strongly discouraged: non-const global variable definition (with initializer)
x = 5;         // compile error: executable statements are not allowed in namespaces

int main()     // okay: function definition
{
    return 0;
}

void goo();    // okay: A function forward declaration
```
- a good rule of thumb is to not define variables in the global namespace which makes sense 

- OHHH so the "std" shit we've been using is a namespace
> The most straightforward way to tell the compiler that we want to use cout from the std namespace is by explicitly using the std:: prefix
> Use explicit namespace prefixes to access identifiers defined in a namespace.
```cpp
#include <iostream> // imports the declaration of std::cout into the global scope

using namespace std; // makes std::cout accessible as "cout"

int cout() // defines our own "cout" function in the global namespace
{
    return 5;
}

int main()
{
    cout << "Hello, world!"; // Compile error!  Which cout do we want here?  The one in the std namespace or the one we defined above?

    return 0;
}
``` 
- huh. okay, so we should generally not use the std namespace and just use the :: (this is a scope resolution operator ::)
```cpp
#include <iostream> // imports the declaration of std::cout into the global scope

void foo(int x) // foo is defined in the global scope, x is defined within scope of foo()
{ // braces used to delineate nested scope region for function foo()
    std::cout << x << '\n';
} // x goes out of scope here

int main()
{ // braces used to delineate nested scope region for function main()
    foo(5);

    int x { 6 }; // x is defined within the scope of main()
    std::cout << x << '\n';

    return 0;
} // x goes out of scope here
// foo and main (and std::cout) go out of scope here (the end of the file)
```
- okay also they talked about namespaces but didnt fucking tell me how to make and use a namespace likeee okay bro i guess 

## 2.10

- what is a preprocessor?
> When the preprocessor runs on this program, the preprocessor will replace #include <iostream> with the contents of the file named “iostream” and then preprocess the included content and the rest of the file.
- and such files like iostream are usually built into the compiler or something? 
- so thats the include preprocessor, heres about the #define preprocessor
- it seems to just be to replace text. it defines a macro,  a macro is a rule that defines how input text is converted into replacement output text.
```cpp
#define IDENTIFIER
#define IDENTIFIER substitution_text
```
> Macro names should be written in all uppercase letters, with words separated by underscores.
- it simpoly replaces wherever it sees "IDENTIFIER" with substitution_text, so an example is:
```cpp
#define LYNZL "goat of sword pvp" 

int main() {
    std::cout << "my name is: " << LYNZL << "\n";
    return 0;
}
```
- and the output would be my name is: the goat of sword pvp
> Avoid macros with substitution text unless no viable alternatives exist.
- so like why tell me u could do that when i should avoid it

> Object-like macros can also be defined without substitution text. like `define LYNZL`
- this is acceptable to use, and how you can use it is as follows:
> The conditional compilation preprocessor directives allow you to specify under what conditions something will or won’t compile. There are quite a few different conditional compilation directives, but we’ll only cover a few that are used the most often: #ifdef, #ifndef, and #endif.
- FINALLY, SOME KIND OF IF ELSE STATEMENT, but this seems to be really shitty to use since we can only #define stuff, in the code and cant allow the user to #define stuff 
```cpp
#include <iostream>

#define PRINT_JOE

int main()
{
#ifdef PRINT_JOE
    std::cout << "Joe\n"; // will be compiled since PRINT_JOE is defined
#endif

#ifdef PRINT_BOB
    std::cout << "Bob\n"; // will be excluded since PRINT_BOB is not defined
#endif

    return 0;
}
```
- like genuinely what is the uses here 
```cpp
#include <iostream>

int main()
{
#ifndef PRINT_BOB
    std::cout << "Bob\n";
#endif

    return 0;
}
```
- ifndef checks if something is NOT defined, and does something if it is true that it isnt defined
- ifdef checks if something is defined
- endif is just to mark where the code that should be excluded ends

- one more use is to comment out stuff or tell the compiler to not compile stuff like:
```cpp
#include <iostream>

int main()
{
    std::cout << "Joe\n";

#if 0 // Don't compile anything starting here
    std::cout << "Bob\n";
    /* Some
     * multi-line
     * comment here
     */
    std::cout << "Steve\n";
#endif // until this point

    return 0;
}
```
- by using #if 0. AND if you want to temporarily compile that stuff you can do #if 1 and #if 1 always is true 

```cpp
#define FOO 9 // Here's a macro substitution

#ifdef FOO // This FOO does not get replaced with 9 because it’s part of another preprocessor directive
    std::cout << FOO << '\n'; // This FOO gets replaced with 9 because it's part of the normal code
#endif
```
- also it is worth to note that preprocessor directives dont care what theyre in, theyre always compiled and have no scope or all the scope?
- theyre compile top to bottom
- you can have multiple files to define stuff in and then include it in your main file:
alex.h
` #define MY_NAME "Alex" `
main.cpp
```cpp
#include "Alex.h" // copies #define MY_NAME from Alex.h here
#include <iostream>

int main()
{
	std::cout << "My name is: " << MY_NAME << '\n'; // preprocessor replaces MY_NAME with "Alex"

	return 0;
}
```
- and that prints my name is alex

- also #define seems to be completely local
> This means that directives are only valid from the point of definition to the end of the file in which they are defined. Directives defined in one file do not have any impact on other files (unless they are #included into another file). For example:
- say you have a file, function.cpp
```cpp
#include <iostream>

void doSomething()
{
#ifdef PRINT
    std::cout << "Printing!\n";
#endif
#ifndef PRINT
    std::cout << "Not printing!\n";
#endif
}
```

- and you have a file main.cpp
```cpp
void doSomething(); // forward declaration for function doSomething()

#define PRINT

int main()
{
    doSomething();

    return 0;
}
```

- this will print Not Printing! why? because when the function is ran in the main.cpp file it treats the function like those #ifdef or #ifndef lines just didnt exist at all 
> Even though PRINT was defined in main.cpp, that doesn’t have any impact on any of the code in function.cpp (PRINT is only #defined from the point of definition to the end of main.cpp). This will be of consequence when we discuss header guards in a future lesson.

## 2.11

> In this example, we used a forward declaration so that the compiler will know what identifier add is when compiling main.cpp. As previously mentioned, manually adding forward declarations for every function you want to use that lives in another file can get tedious quickly.
- so you can use a header file to supposedly put all your forward declarations in. thats very cool 

> Do not put function and variable definitions in your header files (for now).

> Defining either of these in a header file will likely result in a violation of the one-definition rule (ODR) if that header is then #included into more than one source (.cpp) file.

> Source files should #include their paired header file (if one exists).
- this is why i included the header in app.cpp as u can see
- also dont include .cpp files 
> It’s common that the content of a header file will make use of something that is declared (or defined) in another header file. When this happens, the header file should #include the other header file containing the declaration (or definition) that it needs.
- yeah ok i havent had to use this yet so heres an example:
foo.h
```h
#include <string_view> // required to use std::string_view

std::string_view getApplicationName(); // std::string_view used here
```
> When your source (.cpp) file #includes a header file, you’ll also get any other header files that are #included by that header (and any header files those include, and so on).
- lol ok 
> Each file should explicitly #include all of the header files it needs to compile. Do not rely on headers included transitively from other headers.

> To maximize the chance that missing includes will be flagged by compiler, order your #includes as follows (skipping any that are not relevant):
>
>> The paired header file for this code file (e.g. add.cpp should #include "add.h")
>>Other headers from the same project (e.g. #include "mymath.h")
>>3rd party library headers (e.g. #include <boost/tuple/tuple.hpp>)
>>Standard library headers (e.g. #include <iostream>)
>>The headers for each grouping should be sorted alphabetically (unless the documentation for a 3rd party library instructs you to do otherwise).

## 2.12

- header guards!? 
- now we know that both variable identifier or a function identifier can only be defined once across all files (not including declarations)
- its quite easy to get in a situation where a header file is included more than once
- all of your header files should have header guards on them to ensure all the headers only get loaded once, you can do this with the following example:
```h
square.h
#ifndef SQUARE_H
#define SQUARE_H

int getSquareSides()
{
    return 4;
}

#endif

wave.h
#ifndef WAVE_H
#define WAVE_H

#include "square.h"

#endif
```

- so when you have the code: 
```cpp
#include "square.h"
#include "wave.h"

int main()
{
    return 0;
}
```
- this will compile now because square.h gets included once, and SQUARE_H is defined, so because of the #ifndef, it wont let it define a secoond time, after the preprocessor does its thing, this is what gets sent to the compiler:
```cpp
// Square.h included from main.cpp
#ifndef SQUARE_H // square.h included from main.cpp
#define SQUARE_H // SQUARE_H gets defined here

// and all this content gets included
int getSquareSides()
{
    return 4;
}

#endif // SQUARE_H

#ifndef WAVE_H // wave.h included from main.cpp
#define WAVE_H
#ifndef SQUARE_H // square.h included from wave.h, SQUARE_H is already defined from above
#define SQUARE_H // so none of this content gets included

int getSquareSides()
{
    return 4;
}

#endif // SQUARE_H
#endif // WAVE_H

int main()
{
    return 0;
}
```
- note: do not put any function definitions in header files cuz if you do youll likely break the ODR rule and it wont coompile? 
> Can’t we just avoid definitions in header files?

We’ve generally told you not to include function definitions in your headers. So you may be wondering why you should include header guards if they protect you from something you shouldn’t do.

There are quite a few cases we’ll show you in the future where it’s necessary to put non-function definitions in a header file. For example, C++ will let you create your own types. These custom types are typically defined in header files, so the type definitions can be propagated out to the code files that need to use them. Without a header guard, a code file could end up with multiple (identical) copies of a given type definition, which the compiler will flag as an error.

So even though it’s not strictly necessary to have header guards at this point in the tutorial series, we’re establishing good habits now, so you don’t have to unlearn bad habits later.

- huh. okay

- assignment: add header guards to: `int add(int x, int y);`
```cpp
#ifndef ADD
#define ADD

int add(int x, int y);

#endif
```
- that should work
- apparently you can use  #pragma once instead of all of those but for chip dev you should use #ifndef, but if i do get hired in quant ill get to use #pragma once

## 2.13

> Spending a little time up front thinking about how to structure your program will lead to better code and less time spent finding and fixing errors.

- this subchapter isreally just talking about planning how to build a program and i already know this or have an intuition for this so im good on this
- an example they gave is:

```cpp
int main()
{
    // Get first number from user
//    getUserInput();

    // Get mathematical operation from user
//    getMathematicalOperation();

    // Get second number from user
//    getUserInput();

    // Calculate result
//    calculateResult();

    // Print result
//    printResult();

    return 0;
}
```
- and then you actually code, which all makes sense 

## 2.x

- now we have a quiz (which i will quickly go do and put in the main.cpp for chapter 02)
