## 1.1
- a statement is an instruction that makes the program do something
- if a line ends in a ; its probably a statement
- a function is a group of statements that get executed top to bottom, seems useful 
>  For example, a function named max might contain statements that figures out which of two numbers is larger. A function named calculateGrade might calculate a student’s grade from a set of test scores. A function named printEmployee might print an employee’s information to the console. We will talk a lot more about functions soon, as they are the most commonly used organizing tool in a program.
> Every C++ program must have a special function named main (all lower case letters).
- a sequence of characters is a string! (recruiter: show me how you would reverse a string or we will shoot your entire family)

- so what the fuck does the hello world code consist of?
```cpp
#include <iostream>

int main()
{
   std::cout << "Hello world!";
   return 0;
}
```

- the line where "include" is is called a preprocessor directive? ok so its just telling the compiler that we're using the iostream library and we needed that to use std::cout cuz that is a part of that library
- int main() tells the compiler we're writing a function whos identifier is main, and as we saw above, every cpp file needs a main function to compile, and apparently the reason for typing int is because  "the function will produce a value whos type is int (an integer)" which i dont really get 
- the {} just tell the compiler where the main function starts and ends, self explanatory
- std::cout is the first statement and cout is apparently "character output" and typing `<<` after std::cout is apparently telling it to display the string on the console 
- and return 0; just means the program has finished running i guess, what happens if i dont type return 0;? lemme go test that
- i tested it, nothing really happened? the code ran fine without the return 0;? 
- apparently ONLY for the main function, you can omit the return 0; and nothing happens, but if you omit it on other functions, it wont end well. and its considered good practice to just add the return 0; cuz clear intent and consistency i guess

- omitting a semi colon there just gives me an error in the compiler saying it expected a ; very self explanatory i see 

## 1.2

- to add a comment in c++ you type // and text after, cool 

- ookay they didnt teach me yet but can i do like x = 2*3 and cout x lemme test that
- when i type ` x = 2*8; ` its coming up underlined, maybe you have to declare it as an integer or a string, x is an integer so lemme try `int x = 2*8;` 
- lmao this worked
```cpp
    int x = 2*8;
    std::cout << x;
```
- i am a genius
- and i just discovered you can put multiple << on one line to print multiple stuff like 
- i wonder what else you can define uhh could you do `string x = "hello?";` maybe idk ok lemme get back to the site

## 1.3 

- values that are directly placed into the code are called literals, like `std::cout << 5;` would literally print 5, if you typed in like a letter thatd be a variable and if u tried to type in a word without quotations thatd just be syntax error
- an object stores a value in memory or something
- ok theyre teaching me variable creation in this sub chapter but i kinda already figured that out earlier lmao
- you can define stuff like words as variables, an example given was `double width; // define a variable named width, of type double` idk what a double is but ok   
- you can define multiple variables on the same line apparently
```cpp
int a, double b; // wrong (compiler error)

int a; double b; // correct (but not recommended)

// correct and recommended (easier to read)
int a;
double b;
```

## 1.4 

- you can assign variables values, which i also just came across earlier lol
```cpp
int width; // define an integer variable named width
width = 5; // assignment of value 5 into variable width

// variable width now has value 5
```
- simple enough 
- waaaiiit does this mean i can do something like
```cpp
int main()
{
    int width;
    width = 5;
    std::cout << width << "\n";
    width = 2*width;
    std::cout << width << "\n";
}
```
- shit yeah i can, awesome
> One downside of assignment is that assigning a value to a just-defined object requires two statements: one to define the variable, and another to assign the value.
- yeah i just experienced that, had to do 2 lines, but apparnetly you can use "initialization" or rather just type `int width { 5 };` to start the variable off with a value of 5 so my code would be shortened to this:
```cpp
    int width { 5 };
    std::cout << width << "\n";
    width = 2*width;
    std::cout << width << "\n";
```
- saves a line, cool
- aaaand they just overcomplicated this shit, why are there so many "forms of initialization"
```cpp
int a;         // default-initialization (no initializer)

// Traditional initialization forms:
int b = 5;     // copy-initialization (initial value after equals sign)
int c ( 6 );   // direct-initialization (initial value in parenthesis)

// Modern initialization forms (preferred):
int d { 7 };   // direct-list-initialization (initial value in braces)
int e {};      // value-initialization (empty braces)
```
> You may see the above forms written with different spacing (e.g. int b=5; int c(6);, int d{7};, int e{};). Whether you use extra spaces for readability or not is a matter of personal preference.
- ok im taking a short break here (continue reading from Copy-initialization)

- im back i dont get why there are so many initialization methods but i think ill just like use list initialization 
- one of the benefits: 
```cpp
int main()
{
    // An integer can only hold non-fractional values.
    // Initializing an int with fractional value 4.5 requires the compiler to convert 4.5 to a value an int can hold.
    // Such a conversion is a narrowing conversion, since the fractional part of the value will be lost.

    int w1 { 4.5 }; // compile error: list-init does not allow narrowing conversion

    int w2 = 4.5;   // compiles: w2 copy-initialized to value 4
    int w3 (4.5);   // compiles: w3 direct-initialized to value 4

    return 0;
}
```
- so like writing list initialization is safer cuz it just doesnt even let you compile if it cant hold the data but if u use the others like = or () then itll compile but youll lose 0.5 and losing data is bad
- so what is `int e {};` from earlier? its when a special form of list-initialization called value-initialization takes place. In most cases, value-initialization will implicitly initialize the variable to zero (or whatever value is closest to zero for a given type). In cases where zeroing occurs, this is called zero-initialization. 
- so basically initializing it but with no value i guess, i dont see the point 
> Prefer direct-list-initialization or value-initialization to initialize your variables.
- you prefer value-initialization when you're going to set the value of the variable very sooon so you dont need to start it out with a value already
- also: std::cin >> x; lets you type a value for x in the console i think, cin is probably character input
- as seen earlier you can initialize multiple variables in a single line
```cpp
int e { 9 }, f { 10 };     // direct-list-initialization
int i {}, j {};            // value-initialization
```
- but you need to give each variable its own initialization you cant just do like e, f {10};
- if youve defined a variable say x{10} and didnt use it anywhere youd get a warning in the compiler probably
- either remove it or use it somewhere
- also theres this [[maybe_unused]] attribute u can add before the variable if u dont want the compiler complaining about not using the variable like 
```cpp
    [[maybe_unused]] int x {223};
```
- and it wont complain
- also it seems the `double` instead of `int` is used to store fractional values in variables, so you could do `double gravity {9.8};` and thatd work but if you did int instead of double itd be an error 

## 1.5

- finally we use the iostream library, the `include <iostream>` stuff at the start
- std::cout << prints to the console and you can use multiple << in a line
- you can use std::endl instead of \n so like: 
```cpp
std::cout << "hi\n";
std::cout << "my name is alex"; 
// would have the same output as
std::cout << "hi" << std::endl;
std::cout << "my name is alex" <<std::endl; 
```
- it is also always good practice to add the second std::endl on the second line just cuz itd represent a "complete thought"
> Output a newline whenever a line of output is complete.

> Using std::endl is often inefficient, as it actually does two jobs: it outputs a newline (moving the cursor to the next line of the console), and it flushes the buffer (which is slow). If we output multiple lines of text ending with std::endl, we will get multiple flushes, which is slow and probably unnecessary.
- so why the fuck did you teach me it i wouldve gotten by using \n

> Prefer \n over std::endl when outputting text to the console. 
- i guess bro

- std::cin >> x;
- it will ask you to enter a number and save it as the variable x, very useful, you could probably make a calculator with this, let me try to make a simple number adder withuot any help
```cpp
    double x{}; 
    std::cout << "enter number 1: ";
    std::cin >> x; 
    double y{};
    std::cout << "enter number 2: ";
    std::cin >> y;
    double z = x + y;
    std::cout << "The sum of the two numbers you entered is " << z << "\n";
```
- shit that was easy and i think the way i went about it is efficient aswell
- oh shit you can apparently take any amount of variables with cin with a space between them, lets try three and add them
```cpp
    double x{}, y{}, z{}; 
    std::cout << "enter three numbers separated by spaces: ";
    std::cin >> x >> y >> z;
    double sum = x + y + z;
    std::cout << "The sum of the three numbers you entered is " << sum << "\n";
```
- easy light work 

```cpp
int x{};
std::cin >> x;
```
- Here’s what happens in a three different input cases:

- If the user types 5a and enter, 5a\n will be added to the buffer. 5 will be extracted, converted to an integer, and assigned to variable x. a\n will be left in the input buffer for the next extraction.
- If the user types ‘b’ and enter, b\n would be added to the buffer. Because b is not a valid integer, no characters can be extracted, so this is an extraction failure. Variable x would be set to 0, and future extractions will fail until the input stream is cleared.
- If std::cin is not in a good state due to a prior failed extraction, nothing happens here. The value of variable x is not altered.

- good to know
- a bit of detail on how cin extracts: it goes from left to right, if it sees 1232bc itll extract 1232 if it sees 56a7 itll only get 56, itll just stop when it hits a letter, atleast when your value is defined as an int or a double or whatever, if your variable is defined as something like a string or idk then it might extract the whole thing 
> Any non-extracted characters (including newlines) remain available for the next extraction attempt.
- idk how youd do this, next extraction attempt? lemme try something 
- so is it possible to get a cin input like 438bac912 and when its int or double itd obviously only collect 438 but is it possible to retrieve what was omitted and print that too somehow? 
```cpp
    int x{}; 
    std::string remaining{};
    std::cout << "enter a number: ";
    std::cin >> x;
    std::cin >> remaining;
    std::cout << "The number you entered is " << x << "\n";
    std::cout << "heres what cin omitted: " << remaining << "\n";
```
- i came up with this, but is there a way to like get just the letters like say you have 23ba8 and you wanna remove the letter junk to have 238 and save 238 as a variable to do something with it later?
- okay i realise im going down a rabbithole when i havent even been officially told about std::string lol 

## 1.6

- make sure to never use uninitialized variables in your code or you will explode (if we're not setting a value atleast just do x{};)
- "implementation defined behaviour" lets the compiler determine how the language will behave, gotta avoid that too, an example is checking the size of an int value, its 4 on some pcs and 2 on some
> Avoid implementation-defined and unspecified behavior whenever possible, as they may cause your program to malfunction on other implementations.

## 1.7

- c++ has a bunch of words it reserved for the language i guess, i am not noting those down fuck that
- variable naming practices: you can do `variable` or for multiple words `thisIsVariable` or `this_is_variable` it doesnt really change anything but good to know i guess
> When working in an existing program, use the conventions of that program (even if they don’t conform to modern best practices). Use modern best practices when you’re writing new programs.
- do NOT start names with underscores, cant start with a number either, be detailed or descriptive thats pretty much it i guess
- fuck these people telling me to write readable code only person that is gonna be able to read my code is either me or satan

## 1.8

- the directives like #include <> stuff must be placed on their own lines at the start
- uhh whitespaces, be careful, sometimes it doesnt matter other times it does, self explanatory i think im too lazy to note 
- for example: ` int main(){std::cout<<"Hello world";return 0;} ` this is valid, the only mandatory whitespace is like between int and main or int and a variable name, all other whitespaces or line breaks r completely ignored 
```cpp
int main()
{
    std::cout << "This is a really, really, really, really, really, really, really, "
        "really long line\n"; // one extra indentation for continuation line

    std::cout << "This is another really, really, really, really, really, really, really, "
                 "really long line\n"; // text aligned with the previous line for continuation line

    std::cout << "This one is short\n";
}
```
- this works ^ 
- summary: format well unless you're a psycho 
```cpp
int main(){int x{};std::string remaining{};std::cout << "enter a number: ";std::cin >> x;std::cin >> remaining;std::cout << "The number you entered is " << x << "\n";std::cout << "heres what cin omitted: " << remaining << "\n";}
```
- you really can just write all your code in one line if you want huh, the only spaces needed are between int and function/variable

## 1.9

- "literals and operators" ok buddy

```cpp
#include <iostream>

int main()
{
    std::cout << 5 << '\n'; // print the value of a literal

    int x { 5 };
    std::cout << x << '\n'; // print the value of a variable
    return 0;
}
```
- yeah makes sense 

- note: c knows bodmas
- all the simple math operators we know - + * / are in c as an operator
- bonus: std::cout << (x=5); prints 5 because no matter what x is, "=" is the assignment operator and it assigns the value 5
- theyll probably tell us more about more operators later and what they do

- do i have enough to make a calculator yet? i could do single case calculator like any one mathematical operator but id need if else statements at least in order to pick the operators im guessing

## 1.10
